# Algorithm parity

The mobile and Python implementations share the V4 factor identifiers, four action scores (88/62/38/12), and default energy-to-plan rule. `backend/tests/test_api.py` covers the HTTP boundary. A full generated-fixture parity runner is intentionally marked for the next pass; the original simulations remain preserved as executable references.
